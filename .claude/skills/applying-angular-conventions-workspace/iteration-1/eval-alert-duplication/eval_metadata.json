{
  "eval_id": 2,
  "eval_name": "alert-duplication",
  "prompt": "J'ai ce pattern répété dans 3 composants Angular différents:\n\n```html\n@if (error()) {\n  <div class=\"alert error\">{{ error() }}</div>\n}\n```\n\nQue faire pour améliorer ça ?",
  "assertions": [
    { "text": "Recommends extracting a standalone reusable AlertComponent" },
    { "text": "The proposed component uses input() signal for message" },
    {
      "text": "The component encapsulates the @if(message()) — callers don't need the @if"
    },
    {
      "text": "No duplicate 'alert' CSS class (avoids [class]=\"'alert ' + type()\" anti-pattern)"
    }
  ]
}
